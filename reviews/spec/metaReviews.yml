config:
  target: "http://localhost:3000"
  phases:
    - duration: 1
      arrivalRate: 1000
      name: meta-reviews end-point
scenarios:
  - flow:
      - get:
          url: "/reviews/meta?product_id=62"